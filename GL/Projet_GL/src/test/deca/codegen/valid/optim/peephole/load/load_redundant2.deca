// Test : load_redundant2
// But : tester la performance de Peephole optimizer dans le cas 
// de load redandants (Load x, Ri) interompue par un println
// Couvre : les séquences de type STORE Ri, X ... LOAD X, Ri
// Résultat attendu : 42


{
    int x = 42;
    int y;

    y = x;
    y = x;

    println(y);
}

