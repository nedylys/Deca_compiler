// Description:
//    Test métamorphique : comparaisons inversées (version 2).
//    Doit produire le même résultat que mr_comparison_inversion_1.deca
//    en utilisant les négations : !(a >= b) au lieu de a < b, etc.
//
// Resultats:
//    Pas d'erreur. Affiche : ok (plusieurs fois)
//    (Identique à mr_comparison_inversion_1.deca)

{
    if (!(3 >= 5)) {
        println("ok") ;
    }

    if (!(1 > 0)) {
        println("pas ok") ;
    }
    else {
        println("ok") ;
    }

    if (!(0 != 0.0)) {
        println("ok") ;
    }

    if (!(0.21 == 0.22)) {
        println("pas ok") ;
    }
    else if (!(0.21 <= 0.22)) {
        println("pas ok") ;
    }
    else {
        println("ok") ;
    }

    if (!(0 < 0)) {
        println("ok") ;
    }
}