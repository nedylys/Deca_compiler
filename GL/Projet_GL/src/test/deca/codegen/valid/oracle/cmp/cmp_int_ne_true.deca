// Test : cmp_int_ne_true
// But : tester l'opérateur != sur int (cas vrai).
// Couvre : CMP int, branchement sur différence.
// Résultat attendu : affiche 1.
{
    int a = 7 ;
    int b = 8 ;
    boolean c = (a != b) ;
    if (c) { 
        println(1) ;  
    } 
    else { 
        println(0) ; 
    }
}
