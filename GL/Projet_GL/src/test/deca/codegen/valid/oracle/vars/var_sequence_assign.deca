// Test : var_sequence_assign
// But : vérifier une séquence d'affectations dépendantes (ordre d'exécution).
// Couvre : stores successifs, dépendances entre variables, rechargements, println.
// Résultat attendu : affiche 6.

{
    int x = 1 ;
    int y = 2 ;

    x = x + y ;   // x = 3
    y = x + y ;   // y = 5
    x = y + 1 ;   // x = 6

    println(x) ;
}
