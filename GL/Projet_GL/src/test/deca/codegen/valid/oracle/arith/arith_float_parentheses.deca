// Test : arith_float_parentheses
// But : vérifier que les parenthèses changent l'ordre de calcul en float.
// Couvre : AST, temporaires, println.
// Résultat attendu : 6.0, -0.5, 0.0.

{
    println((1.0 + 2.0) * 2.0) ;
    println(((2.0 - 0.5) / 1.5) * -0.5) ;
    println(((0.25 + 1.25) - (-1.5 * -1)) / 2.0) ;
}
