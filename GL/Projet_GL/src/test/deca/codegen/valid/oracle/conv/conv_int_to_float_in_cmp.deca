// Test : conv_int_to_float_in_cmp
// But : tester la conversion implicite int -> float dans une comparaison (int vs float).
// Couvre : conversion, CMP float, production bool, branchement if/else.
// Résultat attendu : affiche 1.

{
    int i = 2 ;
    float f = 2.5 ;
    boolean b = (i < f) ;

    if (b) {
        println(1) ;
    } 
    else {
        println(0) ;
    }
}
