// Test : control_while_nested
// But : tester des boucles while imbriquées avec bornes petites.
// Couvre : labels imbriqués, sauvegarde/gestion des variables, ordre exact d'impression.
// Résultat attendu : affiche 0, 1, 10, 11.

{
    int i = 0 ;
    int j ;
    while (i < 2) {
        j = 0 ;
        while (j < 2) {
            println(i * 10 + j) ;
            j = j + 1 ;
        }
        i = i + 1 ;
    }
}
