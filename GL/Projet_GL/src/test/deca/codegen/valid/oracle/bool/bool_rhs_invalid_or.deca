// Test : bool_rhs_invalid_or
// But : vérifier que : si le LHS est true, le RHS ne doit pas être évalué.
// Couvre: génération de ||, prévention d'une évaluation incorrecte.
// Résultat attendu : affiche 1 et le programme ne doit pas du tout planter.

{
    boolean c = true || ((1 / 0) == 0) ;
    if (c) { 
        println(1) ; 
    } 
    else { 
        println(0) ; 
    }
}
