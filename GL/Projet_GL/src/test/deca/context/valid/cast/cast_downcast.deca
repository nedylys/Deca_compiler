// Test : cast_downcast_ok
// But : vérifier qu’un cast explicite A -> B fonctionne quand l’objet réel est bien une instance de B.
// Couvre : downcast valide, vérification dynamique réussie.
// Résultat attendu : compilation sans erreur.
// Exécution attendue : pas d’erreur (b reçoit l’objet).

class A {}
class B extends A {}

{
    A a = new B();
    B b = (B) (a);

}
